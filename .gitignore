# Go build artifacts
/bin/
*.db

# Private nested data repo (hosts, recipes, logs) — never tracked by parent
/data/

# Solid/Vite dependencies and generated bundles
/web/node_modules/
/web/dist/
/web/*.tsbuildinfo
/cmd/devbox-manager/web/
